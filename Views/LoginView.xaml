<Window x:Class="PDVApp.Views.LoginView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:login="clr-namespace:PDVApp.ViewModels"
      Title="Login" Height="200" Width="300">
   <Window.DataContext>
       <login:LoginViewModel/>
   </Window.DataContext>

   <Grid Margin="10">
       <Grid.RowDefinitions>
           <RowDefinition Height="Auto"/>
           <RowDefinition Height="Auto"/>
           <RowDefinition Height="Auto"/>
       </Grid.RowDefinitions>

       <StackPanel>
           <TextBox Margin="0 10" ToolTip="Usuário" Text="{Binding Login}" TextChanged="TextBox_TextChanged" />
           <PasswordBox Margin="0 10" x:Name="SenhaBox" PasswordChanged="SenhaBox_PasswordChanged" />
           <Button Content="Entrar" Command="{Binding EntrarCommand}" />
       </StackPanel>
   </Grid>
</Window>
